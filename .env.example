# Laptop leaderboard API base URL (no trailing slash).
# Local:        http://localhost:8788
# Via tunnel:   https://YOUR-RANDOM-SUBDOMAIN.trycloudflare.com
# KartBlitz uses 8787 — Mini Market uses 8788 so both can run together.
VITE_LB_API_BASE=http://localhost:8788
